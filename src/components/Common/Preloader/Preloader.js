import spin from '../../../img/Preloader/spin.gif'

let Preloader = (props) => {
  return (
    <div style={{ margin: '0, auto' }}>
      <img
        src={spin}
        
      />
    </div>
  )
}

export default Preloader
